/** @type {import('next').NextConfig} */
const nextConfig = {
  // We are leaving this empty to use the default, stable Webpack bundler.
  // The experimental Turbopack configuration has been removed to resolve
  // the JavaScript syntax errors and the __dirname reference error.
};

module.exports = nextConfig;